# RadHydropy
Python code to simulate hydrodynamics. Mainly focus on 1D idealized cases with gravity, radiation, and cosmic rays.

## Documentation

The project documentation is built with Sphinx.

```bash
python -m pip install -e ".[docs]"
sphinx-build -b html docs docs/_build/html
```

Open `docs/_build/html/index.html` after the build finishes.
